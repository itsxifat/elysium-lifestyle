# Capacitor app — only www/, config and package files are tracked.
# The native android/ project and deps are regenerated by `npx cap add android`.
node_modules/
android/
ios/
*.apk
*.aab
*.keystore
.gradle/
